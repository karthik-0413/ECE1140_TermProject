from Resources import Clock

def main():
    # IMPORT ALL MODULES HERE
    
    # USE .show FOR UI MODULES HERE 
    
    # IMPORT CLOCK CLASS HERE
    clock = Clock()
    
    # # Print each of the "tick" signal being emitted from the Clock class
    # self.clock = Clock()
    # # self.clock.tick.connect(lambda second: self.update_time(second))
    # self.clock.tick.connect(lambda second: print(second))
    # self.clock.start_timer()
        
    
    # CALL ALL CONNECT (READ) FUNCTIONS FOR EACH MODULE
    
    while (True):
        pass
        # RUN CLOCK FOR ONE TICK
        
        # CALL ALL WRITE (EMIT) FUNCTIONS FOR EACH MODULE
        
        # RUN CLOCK FOR ONE TICK
        

if __name__ == "__main__":
    main()